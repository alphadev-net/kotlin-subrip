product:
  type: lib
  platforms:
    - jvm

dependencies:
  - $libs.kotlinx.io.core
  - $libs.kotlinx.datetime

test-dependencies:
  - $libs.dachlatten.io

settings:
  kotlin:
    serialization: enabled
  publishing:
    name: kotlin-subrip

apply:
  - ../publish.module-template.yaml